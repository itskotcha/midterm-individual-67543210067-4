# Library Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** นางสาวกชพร วงศ์ใหญ่
- **Student ID:** 67543210067-4
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
โครงสร้างโปรเจกต์แบ่งออกเป็น 3 เลเยอร์หลักเพื่อแยกหน้าที่ความรับผิดชอบ (Separation of Concerns):

- **src/presentation/**: เลเยอร์ที่ติดต่อกับผู้ใช้ รับ HTTP Request และส่ง Response กลับ (Controllers, Routes, Middlewares)
- **src/business/**: เลเยอร์เก็บกฎทางธุรกิจ (Business Logic) ตรวจสอบความถูกต้องของข้อมูล (Services, Validators)
- **src/data/**: เลเยอร์จัดการข้อมูลและติดต่อกับ Database (Repositories, Database Connection)
- **public/**: เก็บไฟล์ Static สำหรับหน้าจอผู้ใช้ (HTML, CSS, JS)

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
1. **Code ยุ่งเหยิง**: โค้ดทั้งหมดรวมอยู่ในไฟล์เดียว (server.js) ทำให้ยากต่อการอ่าน
2. **ยากต่อการบำรุงรักษา**: การแก้ไขโค้ดส่วนหนึ่งอาจกระทบส่วนอื่นได้ง่ายเนื่องจากไม่มีการแยกส่วนที่ชัดเจน
3. **ไม่มี Separation of Concerns**: Logic ของธุรกิจ ปนกับโค้ดจัดการฐานข้อมูลและ HTTP handling
4. **การทำงานร่วมกันทำได้ยาก**: นักพัฒนาหลายคนแก้ไฟล์เดียวกันพร้อมกันทำให้เกิด Conflict บ่อยครั้ง

### วิธีแก้ไขด้วย Layered Architecture:
1. **แบ่งเลเยอร์ตามหน้าที่**: แยกโค้ดออกเป็น Presentation, Business และ Data Layer ชัดเจน
2. **ใช้ Repository Pattern**: แยกโค้ด SQL Queries ออกมาไว้ที่ Data Access Layer
3. **ใช้ Service Layer**: ย้าย Business Rules และ Validation ไปไว้ใน Service เพื่อให้ง่ายต่อการทดสอบ
4. **ใช้ Controller**: ให้ทำหน้าที่เพียงรับส่งข้อมูลและจัดการ HTTP Status Codes เท่านั้น

### ประโยชน์ที่ได้รับ:
1. **Maintainability**: โค้ดเป็นระเบียบ แก้ไขได้ง่ายโดยไม่กระทบส่วนอื่น
2. **Scalability**: สามารถขยายระบบหรือเปลี่ยนฐานข้อมูลได้ง่ายในอนาคต
3. **Reusability**: เลเยอร์ Business Logic สามารถนำไปใช้ซ้ำกับโปรเจกต์อื่นได้
4. **Testability**: สามารถเขียน Test แยกแต่ละส่วนได้ง่ายขึ้น

## 🚀 How to Run

```bash
# 1. Clone repository
git clone https://github.com/itskotcha/midterm-individual-67543210067-4.git

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
```

## 📝 API Endpoints
GET `/api/books` - ดึงหนังสือทั้งหมด (รองรับ Query ?status=)

GET `/api/books/:id` - ดึงรายละเอียดหนังสือรายเล่ม

POST `/api/books` - เพิ่มหนังสือใหม่

PUT `/api/books/:id` - แก้ไขข้อมูลหนังสือ

PATCH `/api/books/:id/borrow` - ทำรายการยืมหนังสือ

PATCH `/api/books/:id/return` - ทำรายการคืนหนังสือ

DELETE `/api/books/:id` - ลบหนังสือ